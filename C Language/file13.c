#include<stdio.h>
/*code for roots for quadratic equation real or imaginary*/
int main()
{

    float a, b, c, d, root1,root2;
    printf("Please enter the value of a, b, c: ");
    scanf("%f %f %f", &a, &b,&c);
    d=b*b-4*a*c;
    if(d>0)
    {
        root1=(-b+sqrt(d))
    }
}